import { navLogo } from "./navLogo"
import { navLinks } from "./navlinks"
import { navActions } from "./navactions"

export const navBar = () => {
    return <section className="container-app sticky top-0 z-50 glass">
        <navLogo />
        <navLinks />
        <navActions />
    </section>
}